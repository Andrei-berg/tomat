---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - .env.local
  - .gitignore
  - supabase/migrations/20260429000000_initial_schema.sql
  - supabase/seed.sql
autonomous: false

requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

user_setup:
  - service: supabase
    why: "Хостинг БД (PostgreSQL) для всех данных приложения"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API → Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API → anon/public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → service_role key (secret)"
    dashboard_config:
      - task: "Создать новый Supabase проект"
        location: "https://supabase.com/dashboard → New Project"
      - task: "Применить миграцию — вставить содержимое supabase/migrations/20260429000000_initial_schema.sql в SQL Editor и запустить"
        location: "Supabase Dashboard → SQL Editor → New query"
      - task: "Применить seed — вставить содержимое supabase/seed.sql в SQL Editor и запустить"
        location: "Supabase Dashboard → SQL Editor → New query"
  - service: iron-session
    why: "Зашифрованные сессионные cookies"
    env_vars:
      - name: IRON_SESSION_SECRET
        source: "Сгенерировать: openssl rand -base64 32 (минимум 32 символа)"
  - service: app
    why: "Единый общий пароль для доступа в приложение"
    env_vars:
      - name: APP_PASSWORD
        source: "Придумать самостоятельно — пароль, который будут вводить продавец и владелец"

must_haves:
  truths:
    - "Next.js 15 проект создан и запускается: npm run dev стартует без ошибок"
    - "Все 6 таблиц созданы в Supabase с включённым RLS (rowsecurity = true)"
    - "Seed загружен: таблица products содержит 6 товаров в правильном порядке"
    - "Файл .env.local содержит все 5 переменных окружения"
    - "Миграция применена: SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public' возвращает 6 строк, все true"
  artifacts:
    - path: "package.json"
      provides: "Next.js 15 + iron-session + @supabase/supabase-js + @supabase/ssr + zod + server-only"
      contains: "iron-session"
    - path: "supabase/migrations/20260429000000_initial_schema.sql"
      provides: "Полная схема БД с RLS на всех 6 таблицах"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "supabase/seed.sql"
      provides: "6 товаров (Пятерка, Шестерка, Семерка, Восьмерка, НС, Хара-Хура)"
      contains: "Хара-Хура"
    - path: ".env.local"
      provides: "Все переменные окружения для работы приложения"
      contains: "IRON_SESSION_SECRET"
  key_links:
    - from: "package.json"
      to: "iron-session"
      via: "npm install"
      pattern: "iron-session"
    - from: "supabase/migrations/20260429000000_initial_schema.sql"
      to: "Supabase Dashboard SQL Editor"
      via: "Manual paste and execute"
      pattern: "ENABLE ROW LEVEL SECURITY"
---

<objective>
Создать scaffolding Next.js 15 проекта, написать файлы миграции Supabase и seed, настроить переменные окружения.

Purpose: Фундамент для всего приложения — без этого ни одна другая фаза не может работать.
Output: Работающий Next.js 15 проект со всеми зависимостями + SQL файлы для применения в Supabase.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@TOMATO_SALES_SNAPSHOT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with all dependencies</name>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, tailwind.config.ts, postcss.config.mjs, .gitignore, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
    Run the following commands from the project root /home/user/Projects/tomat (NOT inside a subdirectory — scaffold in-place or adjust if directory has files):

    ```bash
    # The project root already has TOMATO_SALES_SNAPSHOT.md — scaffold a new Next.js app
    # Use --yes flag and answer prompts: TypeScript=yes, ESLint=yes, Tailwind=yes, src/=yes, App Router=yes, import alias=@/*
    npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*" --eslint
    ```

    After scaffolding, verify Next.js version >= 15.2.3 (CRITICAL — CVE-2025-29927):
    ```bash
    npm list next
    ```

    If version < 15.2.3, upgrade:
    ```bash
    npm install next@latest
    ```

    Install additional dependencies:
    ```bash
    npm install iron-session @supabase/supabase-js @supabase/ssr zod server-only
    npm install -D supabase
    ```

    Add to .gitignore (append if not already present):
    ```
    .env.local
    .env*.local
    ```

    Clean up the default Next.js boilerplate in src/app/page.tsx — replace with a minimal placeholder:
    ```typescript
    // src/app/page.tsx
    export default function Home() {
      return <main className="p-4">Tomat</main>
    }
    ```

    Clean up src/app/globals.css — keep only the Tailwind directives:
    ```css
    @import "tailwindcss";
    ```
    (or if Tailwind v3: `@tailwind base; @tailwind components; @tailwind utilities;`)
  </action>
  <verify>
    <automated>cd /home/user/Projects/tomat && npm list next iron-session @supabase/supabase-js @supabase/ssr zod server-only 2>&1 | grep -E "(next|iron-session|supabase|zod|server-only)"</automated>
  </verify>
  <done>npm list показывает: next >= 15.2.3, iron-session, @supabase/supabase-js, @supabase/ssr, zod, server-only — все установлены без ошибок</done>
</task>

<task type="auto">
  <name>Task 2: Write Supabase migration and seed files</name>
  <files>supabase/migrations/20260429000000_initial_schema.sql, supabase/seed.sql</files>
  <action>
    Create directory supabase/migrations/ and write the following files exactly:

    **supabase/migrations/20260429000000_initial_schema.sql:**
    ```sql
    -- products (fixed list — 6 tomato varieties)
    create table public.products (
      id uuid primary key default gen_random_uuid(),
      name text not null,
      sort_order int not null
    );
    alter table public.products enable row level security;

    -- prices
    create table public.prices (
      id uuid primary key default gen_random_uuid(),
      product_id uuid not null references public.products(id),
      date date not null,
      price_per_kg decimal(10,2) not null,
      unique (product_id, date)
    );
    alter table public.prices enable row level security;

    -- clients
    create table public.clients (
      id uuid primary key default gen_random_uuid(),
      name text not null,
      phone text,
      notes text,
      created_at timestamptz default now()
    );
    alter table public.clients enable row level security;

    -- orders
    create table public.orders (
      id uuid primary key default gen_random_uuid(),
      created_at timestamptz default now(),
      client_id uuid references public.clients(id),
      client_name_raw text,
      payment_type text not null check (payment_type in ('cash','card','debt')),
      calculated_total decimal(10,2),
      discount_percent decimal(5,2) default 0,
      manual_total decimal(10,2),
      status text not null check (status in ('paid','debt','partial')) default 'paid',
      notes text
    );
    alter table public.orders enable row level security;

    -- order_items
    create table public.order_items (
      id uuid primary key default gen_random_uuid(),
      order_id uuid not null references public.orders(id),
      product_id uuid not null references public.products(id),
      boxes_count int not null,
      weight_kg decimal(10,3) not null,
      price_per_kg decimal(10,2) not null,
      line_total decimal(10,2) generated always as (weight_kg * price_per_kg) stored
    );
    alter table public.order_items enable row level security;

    -- debt_payments
    create table public.debt_payments (
      id uuid primary key default gen_random_uuid(),
      order_id uuid not null references public.orders(id),
      amount decimal(10,2) not null,
      paid_at timestamptz default now(),
      payment_type text not null check (payment_type in ('cash','card')),
      notes text
    );
    alter table public.debt_payments enable row level security;
    ```

    **supabase/seed.sql:**
    ```sql
    insert into public.products (name, sort_order) values
      ('Пятерка', 1),
      ('Шестерка', 2),
      ('Семерка', 3),
      ('Восьмерка', 4),
      ('НС', 5),
      ('Хара-Хура', 6);
    ```

    NOTE: Do NOT run these SQL files — they must be applied manually by the user in the Supabase Dashboard SQL Editor (the user does not have local Docker/Supabase CLI set up). The files are written to disk for the user to copy-paste.
  </action>
  <verify>
    <automated>cd /home/user/Projects/tomat && grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260429000000_initial_schema.sql</automated>
  </verify>
  <done>grep returns 6 — exactly 6 tables have RLS enabled in the migration file. supabase/seed.sql exists and contains all 6 product names including Хара-Хура.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Apply Supabase migration and create .env.local</name>
  <files>.env.local</files>
  <action>
    Human-only step: apply the SQL migration in Supabase dashboard and create .env.local.
    Follow the how-to-verify instructions below exactly.
  </action>
  <verify>
    <automated>cd /home/user/Projects/tomat && test -f .env.local && grep -q "IRON_SESSION_SECRET" .env.local && grep -q "APP_PASSWORD" .env.local && echo "env.local OK"</automated>
  </verify>
  <done>.env.local exists with all 5 env vars, Supabase migration applied, 6 products seeded, npm run dev starts without errors</done>
  <what-built>
    - Next.js 15 project scaffolded with all dependencies installed
    - supabase/migrations/20260429000000_initial_schema.sql — полная схема БД с RLS
    - supabase/seed.sql — 6 товаров

    Before proceeding to Plan 02 (session layer), you need to:
    1. Create a Supabase project at https://supabase.com/dashboard
    2. Apply the migration SQL in Supabase Dashboard → SQL Editor
    3. Apply the seed SQL in Supabase Dashboard → SQL Editor
    4. Create .env.local with all credentials
  </what-built>
  <how-to-verify>
    Step 1: Go to https://supabase.com/dashboard → New project → create a project

    Step 2: Open Supabase Dashboard → SQL Editor → New query
    Copy the full contents of supabase/migrations/20260429000000_initial_schema.sql and run it.
    Expected: "Success. No rows returned"

    Step 3: Open Supabase Dashboard → SQL Editor → New query
    Copy the full contents of supabase/seed.sql and run it.
    Expected: "Success. 6 rows affected"

    Step 4: Verify RLS is enabled — run in SQL Editor:
    ```sql
    SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public' ORDER BY tablename;
    ```
    Expected: 6 rows, all with rowsecurity = true

    Step 5: Create /home/user/Projects/tomat/.env.local with these values:
    ```
    NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
    NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...  (from Project Settings → API → anon/public)
    SUPABASE_SERVICE_ROLE_KEY=eyJ...      (from Project Settings → API → service_role — KEEP SECRET)
    IRON_SESSION_SECRET=                  (run: openssl rand -base64 32)
    APP_PASSWORD=                         (your chosen shared password)
    ```

    Step 6: Verify the app starts:
    ```bash
    cd /home/user/Projects/tomat && npm run dev
    ```
    Expected: Server starts on http://localhost:3000, no errors in console
  </how-to-verify>
  <resume-signal>Type "done" when .env.local is created and migration is applied, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After this plan, verify:
1. `npm list next` shows >= 15.2.3
2. `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260429000000_initial_schema.sql` returns 6
3. `.env.local` exists (not committed to git)
4. `npm run dev` starts without errors
5. Supabase SQL Editor: `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public'` returns 6 rows all true
</verification>

<success_criteria>
- Next.js 15.2.3+ installed with iron-session, @supabase/supabase-js, @supabase/ssr, zod, server-only
- 6-table Supabase schema applied with RLS on every table
- 6 products seeded
- .env.local with all 5 required vars
- npm run dev starts cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
