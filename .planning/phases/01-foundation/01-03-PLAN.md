---
phase: 01-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/middleware.ts
  - src/app/actions/auth.ts
  - src/app/(auth)/login/page.tsx
  - src/components/ui/login-form.tsx
  - src/app/page.tsx
autonomous: false

requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "Неаутентифицированный запрос к / перенаправляется на /login (middleware)"
    - "Пользователь вводит правильный APP_PASSWORD и попадает на / (login работает)"
    - "После обновления страницы пользователь остаётся в системе (cookie сохранена)"
    - "Пользователь нажимает «Выйти» и перенаправляется на /login (logout работает)"
    - "Неверный пароль показывает сообщение об ошибке на форме входа"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Route guard — редирект неаутентифицированных на /login"
      contains: "isAuthenticated"
    - path: "src/app/actions/auth.ts"
      provides: "login() и logout() Server Actions"
      exports: ["login", "logout"]
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Страница входа — проверяет сессию, рендерит LoginForm"
      contains: "LoginForm"
    - path: "src/components/ui/login-form.tsx"
      provides: "Клиентская форма входа с useActionState"
      contains: "useActionState"
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/session.ts"
      via: "import getSession"
      pattern: "getSession"
    - from: "src/app/actions/auth.ts"
      to: "src/lib/session.ts"
      via: "import createSession, deleteSession"
      pattern: "createSession|deleteSession"
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/ui/login-form.tsx"
      via: "import LoginForm"
      pattern: "LoginForm"
    - from: "src/components/ui/login-form.tsx"
      to: "src/app/actions/auth.ts"
      via: "import login"
      pattern: "import.*login.*actions"
---

<objective>
Создать middleware для защиты маршрутов, Server Actions для входа/выхода, страницу /login и клиентскую форму. Завершить человеческой проверкой полного auth flow.

Purpose: Реализует все 4 auth требования (AUTH-01..AUTH-04) — это единственная «дверь» в приложение.
Output: Работающий auth: /login форма, сессия в cookie, middleware redirect, logout.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Из Plan 02 — что уже существует и нужно импортировать -->

From src/lib/session.ts:
```typescript
export interface SessionData { isAuthenticated: boolean }
export async function getSession(): Promise<IronSession<SessionData>>
export async function createSession(): Promise<void>
export async function deleteSession(): Promise<void>
```

From src/lib/dal.ts:
```typescript
export const verifySession: () => Promise<{ isAuth: true }>
// Вызывать в каждом Server Component и Server Action для защиты данных
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Middleware, Server Actions and login page</name>
  <files>src/middleware.ts, src/app/actions/auth.ts, src/app/(auth)/login/page.tsx, src/components/ui/login-form.tsx, src/app/page.tsx</files>
  <action>
    **1. Create src/middleware.ts:**
    ```typescript
    import { NextRequest, NextResponse } from 'next/server'
    import { getSession } from '@/lib/session'

    const publicRoutes = ['/login']

    export async function middleware(req: NextRequest) {
      const path = req.nextUrl.pathname
      const isPublic = publicRoutes.some(r => path.startsWith(r))

      const session = await getSession()

      if (!isPublic && !session.isAuthenticated) {
        const loginUrl = new URL('/login', req.url)
        loginUrl.searchParams.set('redirect', path)
        return NextResponse.redirect(loginUrl)
      }

      if (isPublic && session.isAuthenticated) {
        return NextResponse.redirect(new URL('/', req.url))
      }

      return NextResponse.next()
    }

    export const config = {
      matcher: ['/((?!api|_next/static|_next/image|.*\\.png$|.*\\.ico$|.*\\.svg$).*)'],
    }
    ```

    CRITICAL: The matcher MUST exclude _next/static, _next/image, and static assets.
    Without this, every image/CSS request hits middleware and degrades performance.

    **2. Create src/app/actions/auth.ts:**
    ```typescript
    'use server'
    import { redirect } from 'next/navigation'
    import { createSession, deleteSession } from '@/lib/session'

    export async function login(
      state: { error?: string } | undefined,
      formData: FormData,
    ) {
      const password = formData.get('password') as string
      const redirectPath = (formData.get('redirect') as string) || '/'

      if (!password || password !== process.env.APP_PASSWORD) {
        return { error: 'Неверный пароль' }
      }

      await createSession()
      redirect(redirectPath.startsWith('/') ? redirectPath : '/')
    }

    export async function logout() {
      await deleteSession()
      redirect('/login')
    }
    ```

    NOTE: Compare password directly with process.env.APP_PASSWORD — no hashing needed (shared secret in env, never stored in DB). Use strict inequality (!==).

    **3. Create src/app/(auth)/login/page.tsx:**
    ```typescript
    import { redirect } from 'next/navigation'
    import { getSession } from '@/lib/session'
    import LoginForm from '@/components/ui/login-form'

    interface Props {
      searchParams: Promise<{ redirect?: string }>
    }

    export default async function LoginPage({ searchParams }: Props) {
      const params = await searchParams
      const session = await getSession()

      if (session.isAuthenticated) {
        redirect(params.redirect?.startsWith('/') ? params.redirect : '/')
      }

      return (
        <main className="flex min-h-screen items-center justify-center bg-gray-50">
          <LoginForm redirectPath={params.redirect} />
        </main>
      )
    }
    ```

    NOTE: `searchParams` is async in Next.js 15 (must be awaited).
    Call getSession() directly here (not verifySession() — that redirects to /login, creating a loop).

    **4. Create src/components/ui/login-form.tsx:**
    ```typescript
    'use client'
    import { useActionState } from 'react'
    import { login } from '@/app/actions/auth'

    interface Props {
      redirectPath?: string
    }

    export default function LoginForm({ redirectPath }: Props) {
      const [state, action, pending] = useActionState(login, undefined)

      return (
        <form action={action} className="flex flex-col gap-4 w-full max-w-sm p-6 bg-white rounded-xl shadow">
          <input type="hidden" name="redirect" value={redirectPath ?? '/'} />
          <h1 className="text-2xl font-bold text-center">Вход</h1>
          <input
            name="password"
            type="password"
            placeholder="Пароль"
            autoFocus
            required
            className="border rounded-lg px-4 py-3 text-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
          {state?.error && (
            <p className="text-red-500 text-sm text-center">{state.error}</p>
          )}
          <button
            type="submit"
            disabled={pending}
            className="bg-blue-600 text-white rounded-lg px-4 py-3 text-lg font-medium disabled:opacity-50 active:bg-blue-700"
          >
            {pending ? 'Вход...' : 'Войти'}
          </button>
        </form>
      )
    }
    ```

    NOTE: Use `useActionState` from React (not `useFormStatus` — useActionState supersedes it in React 19 / Next.js 15).

    **5. Update src/app/page.tsx** — redirect to /orders (all protected pages will exist later, for now just a placeholder protected page):
    ```typescript
    import { redirect } from 'next/navigation'
    import { verifySession } from '@/lib/dal'

    export default async function Home() {
      await verifySession() // defense-in-depth — called even though middleware also guards this
      redirect('/orders')
    }
    ```

    Note: /orders page doesn't exist yet — this is fine, it will show a 404 after successful login.
    The important thing is that verifySession() is called on the root page.
  </action>
  <verify>
    <automated>cd /home/user/Projects/tomat && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles without errors. All 5 files exist. middleware.ts has proper matcher config. actions/auth.ts has 'use server'. login-form.tsx has 'use client' and useActionState.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete auth flow in browser</name>
  <files>src/middleware.ts, src/app/actions/auth.ts, src/app/(auth)/login/page.tsx</files>
  <action>
    Human verification of the complete auth flow built in Task 1.
    Follow the how-to-verify steps below to confirm all 4 auth requirements work.
  </action>
  <verify>
    <automated>cd /home/user/Projects/tomat && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build passes. In browser: unauthenticated requests redirect to /login, correct password logs in, session persists on refresh, logout works.</done>
  <what-built>
    Complete authentication system:
    - Middleware redirecting unauthenticated requests to /login (AUTH-01)
    - Password login via Server Action with iron-session cookie (AUTH-02, AUTH-03)
    - Logout Server Action (AUTH-04)
    - Login page at /login with password form
    - verifySession() called on protected pages (defense-in-depth vs CVE-2025-29927)
  </what-built>
  <how-to-verify>
    Ensure npm run dev is running: `cd /home/user/Projects/tomat && npm run dev`

    **Test AUTH-01 — Redirect unauthenticated:**
    1. Open a new incognito/private browser window
    2. Visit http://localhost:3000
    3. Expected: redirected to http://localhost:3000/login?redirect=/

    **Test AUTH-02 — Wrong password:**
    1. On /login, enter a wrong password and submit
    2. Expected: "Неверный пароль" error appears, stay on /login

    **Test AUTH-02 + AUTH-03 — Login and session persistence:**
    1. Enter the correct APP_PASSWORD from .env.local
    2. Expected: redirected (to /orders which may show 404 — that is OK)
    3. Now refresh the page (F5)
    4. Expected: still on /orders (or 404), NOT redirected back to /login
    5. Open DevTools → Application → Cookies: should see _tomat_session cookie (httpOnly)

    **Test AUTH-01 — Protected after login:**
    1. While logged in, visit http://localhost:3000/login
    2. Expected: redirected away from /login to /

    **Test AUTH-04 — Logout function exists:**
    Confirm: src/app/actions/auth.ts exports `logout` function that calls deleteSession() + redirect('/login')
    (Full logout UI test deferred to Phase 2 when nav is built)

    **Test — Build check:**
    ```bash
    cd /home/user/Projects/tomat && npm run build 2>&1 | tail -20
    ```
    Expected: Build completes without errors
  </how-to-verify>
  <resume-signal>Type "approved" if AUTH-01, AUTH-02, AUTH-03 work correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After this plan, the full Phase 1 success criteria are met:
1. Unauthenticated request to any route → redirects to /login (AUTH-01)
2. User enters correct password → enters the app (AUTH-02)
3. After page refresh → user stays in the app (AUTH-03, cookie persists)
4. User clicks logout → redirects to /login (AUTH-04)
5. All Supabase tables have RLS enabled (verified in Plan 01)

Run build check: `npm run build` — no TypeScript or build errors
</verification>

<success_criteria>
- middleware.ts: guards all non-public routes, proper matcher
- actions/auth.ts: login() validates APP_PASSWORD, logout() destroys session
- /login page: renders LoginForm, checks existing session to avoid redirect loop
- login-form.tsx: useActionState hook, shows error on wrong password
- Full auth flow works: login, session persistence, logout
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
