---
phase: 03-orders
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/ui/order-form.tsx
autonomous: true
requirements:
  - CLIENT-01
  - CLIENT-02
  - CLIENT-03
  - ORDER-01
  - ORDER-02
  - ORDER-03
  - ORDER-04
  - ORDER-05
  - ORDER-06
  - ORDER-07
  - ORDER-08
  - ORDER-10

must_haves:
  truths:
    - "Форма отображает все 6 товаров; товары без цены серые с заблокированными полями и текстом 'Цена не установлена'"
    - "Итог пересчитывается живьём при вводе веса"
    - "При скидке > 50% показывается жёлтое предупреждение"
    - "При manual_total < 80% от calculated_total — window.confirm перед сохранением"
    - "Тип оплаты 'Долг' требует выбора клиента; кнопка Save неактивна без клиента"
    - "Поиск клиентов работает с debounce 300ms; выпадающий список показывает кнопку 'Добавить' если нет точного совпадения"
    - "После сохранения показываются кнопки 'Новый заказ' и 'К списку заказов'"
  artifacts:
    - path: "src/components/ui/order-form.tsx"
      provides: "Клиентский компонент формы заказа"
      exports: ["default OrderForm"]
  key_links:
    - from: "src/components/ui/order-form.tsx"
      to: "src/app/actions/orders.ts"
      via: "createOrder через useActionState; searchClients/createClient — прямой вызов"
      pattern: "useActionState.*createOrder"
    - from: "handleSubmit"
      to: "buildFormData"
      via: "price_per_kg передаётся как price_${pid} hidden value"
      pattern: "price_"
---

<objective>
OrderForm — клиентский компонент формы создания заказа с полным локальным state.

Purpose: Реализовать весь интерактив формы заказа — ввод позиций, живой итог, скидка, ручной итог, выбор типа оплаты, поиск/создание клиента, валидация кнопки сохранения.
Output: src/components/ui/order-form.tsx — 'use client' компонент, принимающий products и priceMap через props.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-orders/03-CONTEXT.md
@.planning/phases/03-orders/03-RESEARCH.md

<interfaces>
<!-- Props OrderForm получает от Server Component -->
```typescript
interface Product { id: string; name: string; sort_order: number }
interface Props {
  products: Product[]          // 6 товаров из таблицы products, отсортированных по sort_order
  priceMap: Record<string, number>  // product_id → price_per_kg (только товары с ценой на сегодня)
}
```

<!-- Типы из src/app/actions/orders.ts (Plan 01) -->
```typescript
export type CreateOrderState = { error?: string; success?: boolean; orderId?: string } | undefined
export type ClientResult = { id: string; name: string }
export async function createOrder(prev: CreateOrderState, formData: FormData): Promise<CreateOrderState>
export async function searchClients(query: string): Promise<ClientResult[]>
export async function createClient(name: string): Promise<ClientResult | { error: string }>
```

<!-- Паттерн из src/components/ui/prices-form.tsx -->
```typescript
'use client'
import { useActionState, useState, useEffect } from 'react'
// Inline styles с CSS vars: var(--mk-bg), var(--mk-ok-*), var(--mk-err-*), var(--mk-accent)
// maxWidth: 480px, padding: 0 20px, minHeight: 100svh
// Анимации: mkUp, mkPop (CSS keyframes)
```

<!-- Ключевые паттерны из RESEARCH.md -->
// Локальный state — НЕ useFieldArray:
type ItemState = { boxes: string; weight: string }
type ItemsState = Record<string, ItemState>  // keyed by product_id

// Derived values (НЕ хранить в state):
function calcTotal(items: ItemsState, priceMap: Record<string, number>): number
function effectiveTotal(calculated: number, discountPct: string, manualTotal: string): number

// Priority rule (Claude's Discretion):
// manual_total > 0 → абсолютный приоритет; иначе discount_percent; иначе calculated

// canSave:
// hasItems && (!needsClient || hasClient) && !saving

// FormData:
// Плоские имена: weight_${pid}, boxes_${pid}, price_${pid} (снапшот!)
// payment_type, client_id, client_name_raw, discount_percent, manual_total

// Dropdown blur/click fix:
// onMouseDown={e => e.preventDefault()} на элементах dropdown
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrderForm — локальный state, derived values, блок товаров</name>
  <files>src/components/ui/order-form.tsx</files>
  <action>
Создать src/components/ui/order-form.tsx. Это большой 'use client' компонент — реализовать полностью.

**Структура файла:**

1. `'use client'` директива, импорты: useActionState, useState, useEffect, useTransition из 'react'; createOrder, searchClients, createClient из '@/app/actions/orders'; типы CreateOrderState, ClientResult.

2. **Вспомогательные функции (pure, вне компонента):**
   - `calcTotal(items, priceMap)` — сумма price*weight по каждому pid, Math.round до 2 знаков
   - `effectiveTotal(calculated, discountPct, manualTotal)` — приоритет: manual_total если заполнен и >= 0; иначе discount если > 0; иначе calculated
   - `formatRub(amount)` — `amount.toLocaleString('ru-RU', { style: 'currency', currency: 'RUB', maximumFractionDigits: 2 })`

3. **Spinner SVG** — перенести из prices-form (одинаковый):
   ```tsx
   function Spinner() { /* SVG animate-spin 15x15 */ }
   ```

4. **Props interface:**
   ```typescript
   interface Product { id: string; name: string; sort_order: number }
   interface Props { products: Product[]; priceMap: Record<string, number> }
   ```

5. **Локальный state компонента:**
   ```typescript
   const [items, setItems] = useState<Record<string, { boxes: string; weight: string }>>(
     Object.fromEntries(products.map(p => [p.id, { boxes: '', weight: '' }]))
   )
   const [discountPct, setDiscountPct] = useState('')
   const [manualTotal, setManualTotal] = useState('')
   const [paymentType, setPaymentType] = useState<'cash' | 'card' | 'debt'>('cash')
   const [clientQuery, setClientQuery] = useState('')
   const [clientResults, setClientResults] = useState<ClientResult[]>([])
   const [selectedClient, setSelectedClient] = useState<ClientResult | null>(null)
   const [showDropdown, setShowDropdown] = useState(false)
   const [clientCreating, setClientCreating] = useState(false)
   const [isPending, startTransition] = useTransition()
   const [orderState, orderAction, saving] = useActionState(createOrder, undefined)
   ```

6. **Derived values (в теле компонента, не в state):**
   ```typescript
   const calculated = calcTotal(items, priceMap)
   const effective = effectiveTotal(calculated, discountPct, manualTotal)
   const hasItems = Object.values(items).some(i => parseFloat(i.weight) > 0)
   const needsClient = paymentType === 'debt'
   const hasClient = selectedClient !== null
   const canSave = hasItems && (!needsClient || hasClient) && !saving && !isPending
   ```

7. **Debounced search useEffect:**
   ```typescript
   useEffect(() => {
     if (clientQuery.length < 2) { setClientResults([]); setShowDropdown(false); return }
     const timer = setTimeout(async () => {
       const results = await searchClients(clientQuery)
       setClientResults(results)
       setShowDropdown(true)
     }, 300)
     return () => clearTimeout(timer)
   }, [clientQuery])
   ```

8. **handleAddClient** — вызвать createClient(clientQuery), установить selectedClient, закрыть dropdown.

9. **buildFormData()** — собрать FormData:
   - `payment_type`, `client_id` (если выбран), `client_name_raw` (selectedClient.name если выбран)
   - `discount_percent` если заполнен, `manual_total` если заполнен
   - Для каждого pid с weight > 0: `weight_${pid}`, `boxes_${pid}`, `price_${pid}` (из priceMap — СНАПШОТ)

10. **handleSubmit(e):**
    ```typescript
    e.preventDefault()
    // Проверка manual_total < 80%
    const manual = parseFloat(manualTotal)
    if (manualTotal !== '' && !isNaN(manual) && calculated > 0 && manual < calculated * 0.8) {
      const ok = window.confirm(`Итог ${formatRub(manual)} значительно ниже расчётного ${formatRub(calculated)}. Сохранить?`)
      if (!ok) return
    }
    startTransition(() => { orderAction(buildFormData()) })
    ```

11. **JSX layout** — вертикальная прокрутка на телефоне, следует паттерну prices-form (inline styles, CSS vars):

    **Секция 1: Товары** — для каждого product из products:
    - Если `!priceMap[product.id]` → серая карточка, заблокированные inputs, текст «Цена не установлена»
    - Иначе → активная карточка с полями «Ящики» (type=number, min=0, step=1) и «Вес кг» (type=number, min=0, step=0.1)
    - Серым цветом показать цену: `{formatRub(priceMap[product.id])} / кг`
    - Если weight > 0 → показать line_total: `= ${formatRub(price * weight)}`
    - Accent-бордер слева (var(--mk-accent)) если хотя бы один из inputs заполнен

    **Секция 2: Скидка и ручной итог** — два поля рядом или стопкой:
    - Поле «Скидка %» (type=number, min=0, max=100)
    - Если discountPct > 50 → жёлтая полоса: `background: rgba(200,170,0,0.1), border: 1px solid rgba(200,170,0,0.25), color: #c8a200`
    - Поле «Ручной итог ₽» (type=number, min=0)

    **Секция 3: Тип оплаты** — три кнопки-таба: Наличные / Карта / Долг (активный — accent)

    **Секция 4: Клиент** — показывать всегда (не только для долга), но помечать обязательным при Долг:
    - Текстовое поле поиска (value=clientQuery, onChange=setClientQuery)
    - Если выбран selectedClient → показать его имя с кнопкой X для сброса
    - Выпадающий список (position: absolute, z-index: 50):
      - Каждый результат — кнопка с onMouseDown={e=>e.preventDefault()} onClick={()=>setSelectedClient(r)}
      - Последний элемент — кнопка «Добавить "{clientQuery}"» (если нет точного совпадения)
    - onBlur на input: setTimeout(()=>setShowDropdown(false), 150) — чтобы mouseDown успел отработать

    **Секция 5: Итог и сохранение**:
    - Крупный итог: `{formatRub(effective)}` (если effective !== calculated → показать calculated серым)
    - Кнопка «Сохранить заказ» (disabled=!canSave, градиентный фон var(--mk-accent))
    - Если orderState?.error → inline error блок
    - Если orderState?.success → два варианта: кнопка «Новый заказ» (resetForm) и Link «К списку заказов» /orders

12. **resetForm()** — сбросить items, discountPct, manualTotal, paymentType, clientQuery, selectedClient, orderState (через useActionState нельзя напрямую — использовать key prop или отдельный state флаг).

ДИЗАЙН: Следовать паттерну prices-form — тёмный фон var(--mk-bg), карточки с border-radius 12px, padding 16px, gap 12px. Кнопка сохранения — градиент как в prices-form.

ЗАПРЕЩЕНО:
- НЕ хранить effectiveTotal в state
- НЕ использовать useFieldArray или react-hook-form
- НЕ делать onClick на dropdown-элементах (только onMouseDown для blur/click ordering)
- НЕ запрашивать getTodayPrices() внутри компонента — priceMap передаётся через props
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -E "order-form\.tsx" | head -20; echo "TypeScript exit: $?"</verify>
  <done>TypeScript чистый по order-form.tsx; компонент экспортируется как default; canSave-логика покрывает все три условия</done>
</task>

</tasks>

<verification>
npx tsc --noEmit 2>&1 | tail -5
</verification>

<success_criteria>
- src/components/ui/order-form.tsx создан, TypeScript чистый
- Все 6 товаров отрендерены; товары без цены визуально отключены
- calcTotal / effectiveTotal — pure functions вне state
- canSave: hasItems && (!debt || hasClient) && !saving
- window.confirm при manual_total < 80% от calculated
- Жёлтое предупреждение при скидке > 50%
- Dropdown использует onMouseDown, не onClick
- После success orderState — два варианта навигации
</success_criteria>

<output>
После завершения создать `.planning/phases/03-orders/03-02-SUMMARY.md`
</output>
