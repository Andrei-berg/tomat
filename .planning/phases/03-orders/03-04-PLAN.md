---
phase: 03-orders
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
autonomous: false
files_modified:
  - src/app/orders/new/page.tsx
requirements:
  - ORDER-09

must_haves:
  truths:
    - "Переход на /orders/new без цен на сегодня → редирект на /prices"
    - "Переход на /orders/new с ценами → форма OrderForm с товарами и priceMap"
    - "Сохранённый заказ появляется в списке /orders"
    - "Пользователь видит кнопки 'Новый заказ' и 'К списку заказов' после сохранения"
    - "Тип оплаты Долг без клиента — кнопка неактивна"
    - "Скидка > 50% — жёлтое предупреждение"
    - "Ручной итог < 80% от расчётного — диалог подтверждения"
  artifacts:
    - path: "src/app/orders/new/page.tsx"
      provides: "Server Component — точка входа для создания заказа"
      exports: ["default NewOrderPage"]
  key_links:
    - from: "src/app/orders/new/page.tsx"
      to: "src/lib/dal.ts"
      via: "hasTodayPrices() → redirect('/prices'); getTodayPrices() + products query"
      pattern: "hasTodayPrices"
    - from: "src/app/orders/new/page.tsx"
      to: "src/components/ui/order-form.tsx"
      via: "props: products, priceMap"
      pattern: "OrderForm"
---

<objective>
Server Component /orders/new — точка входа для создания заказа + финальная браузерная верификация всей фазы.

Purpose: Склеить DAL (Plan 01) и OrderForm (Plan 02) в работающую страницу. Убедиться, что весь flow от открытия формы до появления заказа в списке работает корректно в браузере.
Output: src/app/orders/new/page.tsx; браузерный тест всех сценариев.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-orders/03-CONTEXT.md
@.planning/phases/03-orders/03-RESEARCH.md
@.planning/phases/03-orders/03-01-SUMMARY.md
@.planning/phases/03-orders/03-02-SUMMARY.md

<interfaces>
<!-- DAL из Plan 01 -->
```typescript
export async function hasTodayPrices(): Promise<boolean>
export async function getTodayPrices(): Promise<TodayPrice[]>
// TodayPrice = { product_id: string; price_per_kg: number }
```

<!-- OrderForm из Plan 02 -->
```typescript
interface Product { id: string; name: string; sort_order: number }
interface Props {
  products: Product[]
  priceMap: Record<string, number>  // только товары с ценой сегодня
}
export default function OrderForm({ products, priceMap }: Props): JSX.Element
```

<!-- Паттерн из prices/page.tsx — точный шаблон для /orders/new -->
```typescript
import { verifySession, hasTodayPrices, getTodayPrices } from '@/lib/dal'
import { createClient as createSupabaseClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
import OrderForm from '@/components/ui/order-form'

export default async function NewOrderPage() {
  await verifySession()
  const hasPrices = await hasTodayPrices()
  if (!hasPrices) redirect('/prices')  // ORDER-09

  const supabase = createSupabaseClient()
  const [{ data: products }, todayPrices] = await Promise.all([
    supabase.from('products').select('id, name, sort_order').order('sort_order'),
    getTodayPrices(),
  ])

  const priceMap: Record<string, number> = Object.fromEntries(
    (todayPrices ?? []).map(p => [p.product_id, p.price_per_kg])
  )
  // products без цены → priceMap не содержит их id → OrderForm показывает как заблокированные

  return (
    <div style={{ minHeight: '100svh', background: 'var(--mk-bg)', fontFamily: 'var(--font-geist-sans)' }}>
      <div style={{ maxWidth: '480px', margin: '0 auto', padding: '0 20px 52px' }}>
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', padding: '24px 0 20px' }}>
          <h1 style={{ fontSize: '22px', fontWeight: 700, color: 'var(--mk-text)' }}>Новый заказ</h1>
          {/* Link ← Заказы */}
        </div>
        <OrderForm products={products ?? []} priceMap={priceMap} />
      </div>
    </div>
  )
}
```

ВАЖНО: import { createClient as createSupabaseClient } — переименовать во избежание конфликта с экспортируемой createClient из orders.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать /orders/new/page.tsx</name>
  <files>src/app/orders/new/page.tsx</files>
  <action>
Создать src/app/orders/new/page.tsx точно по паттерну из секции interfaces выше.

Файл является Server Component (нет 'use client'). Реализовать:

1. Импорт `createClient` из '@/lib/supabase/server' — переименовать локально в `createSupabaseClient` чтобы избежать конфликта с `createClient` Server Action из orders.ts (который на этой странице не импортируется, но конфликт по имени возможен в будущем)

2. `await verifySession()` — обязательно первым вызовом

3. `const hasPrices = await hasTodayPrices()` → если false: `redirect('/prices')`  (ORDER-09)

4. Параллельная загрузка: `Promise.all([products query, getTodayPrices()])`

5. Построить priceMap: только те product_id для которых есть цена. Товары без цены останутся не в priceMap → OrderForm отобразит их заблокированными (ORDER-10 реализован в OrderForm).

6. Рендер: обёртка с `minHeight: '100svh', background: 'var(--mk-bg)'`, внутри `maxWidth: 480px` контейнер с заголовком «Новый заказ», Link «← Заказы» (href='/orders') и `<OrderForm products={...} priceMap={...} />`

7. Добавить link в nav или header для перехода /orders → /orders/new (кнопка «+ Новый заказ» уже есть в Plan 03 на /orders/page.tsx, дополнительно добавить nav-ссылку на главный layout или оставить как есть — на усмотрение исполнителя)

ЗАПРЕЩЕНО:
- НЕ вызывать createOrder или другие мутации на этой странице — только чтение
- НЕ передавать priceMap как undefined — всегда Record&lt;string, number&gt; (пустой если нет цен, хотя redirect гарантирует наличие хотя бы одной)
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -E "new/page\.tsx" | head -10; echo "Exit: $?"</verify>
  <done>TypeScript чистый; redirect при hasPrices=false; OrderForm получает products и priceMap</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Браузерная верификация полного flow создания заказа</name>
  <action>Запустить npm run dev и вручную проверить все сценарии фазы 3 согласно шагам ниже</action>
  <verify>Пользователь подтверждает корректность работы всех сценариев</verify>
  <done>Все сценарии из how-to-verify прошли без ошибок; пользователь написал "approved"</done>
  <what-built>
Полный flow создания заказа:
- /orders/new (форма с 6 товарами, живой итог, поиск клиента, тип оплаты)
- createOrder Server Action (DAL + DB insert)
- /orders (список заказов за день с итогом)
- /orders/[id] (детали заказа с позициями)
- Редирект на /prices при отсутствии цен
  </what-built>
  <how-to-verify>
1. Запустить: npm run dev
2. Перейти /orders/new — проверить что форма открывается с 6 товарами и ценами
3. Ввести вес для 2-3 товаров — итог должен пересчитываться в реальном времени
4. Выбрать тип оплаты «Долг» без клиента — кнопка «Сохранить» должна быть неактивна
5. Найти/создать клиента — кнопка должна активироваться
6. Сохранить заказ — проверить появление кнопок «Новый заказ» и «К списку заказов»
7. Перейти /orders — заказ должен появиться в списке с правильной суммой и типом оплаты
8. Открыть /orders/[id] — проверить позиции, клиента, тип оплаты и итог

Дополнительные сценарии:
- Ввести скидку 60% — жёлтое предупреждение под полем
- Ввести manual_total сильно ниже расчётного (например 100 при итоге 5000) — window.confirm
- Открыть /orders/new без цен на сегодня (очистить /prices) — редирект на /prices
  </how-to-verify>
  <resume-signal>Напишите "approved" или опишите найденные проблемы</resume-signal>
</task>

</tasks>

<verification>
npm run build 2>&1 | tail -20
</verification>

<success_criteria>
- /orders/new открывается и показывает форму с товарами
- Живой итог работает при вводе
- Долг без клиента: кнопка disabled
- Заказ сохраняется и появляется в /orders
- /orders/[id] показывает позиции
- Скидка > 50% — предупреждение; manual_total < 80% — confirm
- Редирект на /prices при отсутствии цен (ORDER-09)
- npm run build без ошибок TypeScript
</success_criteria>

<output>
После завершения создать `.planning/phases/03-orders/03-04-SUMMARY.md`
</output>
