# Phase 3: Orders - Context

**Gathered:** 2026-04-30
**Status:** Ready for planning

<domain>
## Phase Boundary

Создание заказа (форма /orders/new) и просмотр списка заказов (/orders). Включает выбор/создание клиента прямо в форме. Управление долгами, история долгов, отчёты — отдельные фазы.

</domain>

<decisions>
## Implementation Decisions

### Ввод позиций заказа
- Все 6 товаров отображаются сразу (аналогично форме цен), пустые строки игнорируются при сохранении
- Расчёт суммы строки: цена × вес; ящики — справочное поле, не влияют на сумму
- Цена товара видна в строке серым цветом (подтверждает, что система взяла правильную цену)
- Товар без цены на сегодня: строка серая, поля ввода заблокированы, с пояснением («Цена не установлена»)
- Итог пересчитывается в реальном времени при вводе
- После сохранения: показывать два варианта — «Новый заказ» и «К списку заказов»

### Выбор и создание клиента
- Поиск по имени: текстовое поле с выпадающим списком совпадений
- Если клиента нет в базе — в выпадающем списке появляется кнопка «Добавить "Иванов"»; клиент создаётся без перехода на другую страницу
- Клиент обязателен только при типе оплаты «Долг» (кнопка сохранения неактивна без клиента)
- При наличных/карте — клиент опционален
- Клиент с существующим долгом выбирается без предупреждений; баланс долга — фаза 4

### Список заказов (/orders)
- По умолчанию — заказы текущего дня; переключатель для просмотра других дней (вчера, конкретная дата)
- Строка заказа: время создания | имя клиента (или «—» для анонимных) | сумма | тип оплаты (бейдж)
- Внизу списка — итог дня с разбивкой по типам оплаты: Нал / Карта / Долг
- Нажать на строку заказа → страница детали /orders/[id] (состав позиций, клиент, тип оплаты, итог)

### Скидка и ручной итог
- Оба поля независимы и существуют одновременно в форме
- Расположены в секции под блоком позиций, перед полями оплаты и клиента
- Оба сохраняются в БД (discount_percent и manual_total); при конфликте — Claude выбирает логику приоритета
- Предупреждение при скидке >50%: жёлтая полоса под полем скидки (не блокирует)
- Запрос подтверждения при manual_total < 80% от calculated_total: диалог перед сохранением (ORDER-08)

### Claude's Discretion
- Логика приоритета когда заполнены и discount_percent, и manual_total одновременно
- Точный UX переключателя дней в списке заказов
- Анимации и переходы
- Обработка состояний ошибок при сохранении

</decisions>

<specifics>
## Specific Ideas

- Форма заказа — вертикальная прокрутка на телефоне: сначала товары, потом скидка/ручной итог, потом оплата+клиент, потом кнопка сохранения
- Итог заказа показан крупно перед кнопкой сохранения (ключевая цифра)
- Дизайн следует паттерну формы цен: карточки-строки с левым accent-бордером при заполнении, градиентная кнопка «Сохранить»
- «Берут на реализацию» — отдельный сценарий/тип оплаты, зафиксирован в deferred

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- `PricesForm` (src/components/ui/prices-form.tsx): паттерн строк-карточек с live-обновлением, inline styles с CSS vars — прямой шаблон для строк товаров в форме заказа
- `useActionState` + Server Actions: паттерн форм (`savePrices`, `copyYesterdayPrices`) — аналогично для `createOrder`
- Spinner, CheckIcon, ArrowDownIcon SVG-иконки: уже встроены в prices-form, можно переиспользовать
- `verifySession` / `createClient`: auth и Supabase клиент — готовы

### Established Patterns
- Стиль: 100% inline styles с CSS-переменными `var(--mk-*)` — никакого Tailwind, никаких CSS-модулей
- Страницы: Server Component загружает данные → передаёт в Client Component через props
- Server Actions в `/app/actions/` — отдельный файл на фичу
- DAL функции в `/lib/dal.ts` — server-only, через Supabase
- Мобильный макет: `maxWidth: 480px`, `padding: 0 20px`, `minHeight: 100svh`
- Тосты успеха/ошибок: inline блоки с `var(--mk-ok-*)` / `var(--mk-err-*)` и анимацией `mkUp`/`mkPop`

### Integration Points
- `hasTodayPrices()` в dal.ts: уже реализована — использовать для редиректа на /prices при создании заказа без цен (ORDER-09)
- `getTodayPrices()` в dal.ts: возвращает массив {product_id, price_per_kg} — подставляет цены в форму заказа
- Таблицы `clients`, `orders`, `order_items` существуют в схеме БД и `database.ts` типах
- Редирект guard: паттерн `if (!hasTodayPrices) redirect('/prices')` — добавить в /orders/new page

</code_context>

<deferred>
## Deferred Ideas

- **«Берут на реализацию»** — консигнационный тип сделки (товар берут под реализацию, оплата после). Новый тип оплаты / отдельный сценарий — выходит за рамки фазы 3. Зафиксировать в бэклоге.

</deferred>

---

*Phase: 03-orders*
*Context gathered: 2026-04-30*
