---
phase: 03.1-navigation-auth-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/nav-bar.tsx
  - src/app/prices/page.tsx
  - src/app/orders/page.tsx
  - src/app/orders/new/page.tsx
  - src/app/orders/[id]/page.tsx
autonomous: true
requirements:
  - AUTH-04

must_haves:
  truths:
    - "На каждой защищённой странице отображается навигация [Прайс] [Заказы] с подсветкой активного раздела"
    - "Пользователь нажимает кнопку «Выйти» — сессия удаляется, браузер перенаправляется на /login"
    - "NavBar подсвечивает «Заказы» при нахождении на /orders, /orders/new и /orders/[id]"
  artifacts:
    - path: "src/components/ui/nav-bar.tsx"
      provides: "Client Component с usePathname, двумя Link-навигаторами и form action={logout}"
      exports: ["default NavBar"]
    - path: "src/app/prices/page.tsx"
      provides: "Страница /prices с NavBar вместо paddingTop: 48px"
      contains: "<NavBar />"
    - path: "src/app/orders/page.tsx"
      provides: "Страница /orders с NavBar"
      contains: "<NavBar />"
    - path: "src/app/orders/new/page.tsx"
      provides: "Страница /orders/new с NavBar (заменяет ручную «← Заказы» ссылку)"
      contains: "<NavBar />"
    - path: "src/app/orders/[id]/page.tsx"
      provides: "Страница /orders/[id] с NavBar (заменяет «← Назад» ссылку)"
      contains: "<NavBar />"
  key_links:
    - from: "src/components/ui/nav-bar.tsx"
      to: "src/app/actions/auth.ts"
      via: "import { logout } from '@/app/actions/auth' + form action={logout}"
      pattern: "form action=\\{logout\\}"
    - from: "src/app/prices/page.tsx"
      to: "src/components/ui/nav-bar.tsx"
      via: "import NavBar + <NavBar /> в JSX"
      pattern: "<NavBar"
---

<objective>
Создать клиентский компонент NavBar с навигацией между разделами и кнопкой «Выйти», и подключить его на все 4 защищённые страницы.

Purpose: Закрыть AUTH-04 — пользователь сможет выйти из системы с любой страницы. Устранить UX-разрывы: сейчас нет навигации между /prices и /orders без ручного набора URL.

Output: nav-bar.tsx + 4 изменённых страницы
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-navigation-auth-ux/03.1-RESEARCH.md

<interfaces>
<!-- Существующий logout Server Action — уже реализован, не менять -->
From src/app/actions/auth.ts:
```typescript
'use server'
// logout() вызывает deleteSession() + redirect('/login')
export async function logout(): Promise<never>
```

<!-- Существующие страницы — паттерн структуры -->
From src/app/prices/page.tsx:
- Оборачивающий div: minHeight 100svh, background var(--mk-bg), fontFamily var(--font-geist-sans)
- Внутренний div: maxWidth 480px, margin 0 auto, padding 0 20px 52px
- header: paddingTop 48px (УБРАТЬ — NavBar займёт это место), paddingBottom 24px

From src/app/orders/new/page.tsx:
- Текущий header — flex с h1 «Новый заказ» и «← Заказы» Link
- NavBar заменяет этот header полностью (или добавляется перед ним — выбрать при имплементации)

From src/app/orders/[id]/page.tsx:
- header: paddingTop 48px, ссылка «← Назад» сверху с marginBottom 18px
- NavBar добавляется вместо paddingTop 48px; «← Назад» убрать (NavBar даёт доступ к /orders)

<!-- CSS переменные (из src/app/globals.css) -->
var(--mk-accent)       — томатный красный акцент
var(--mk-accent-hi)    — светлее акцента
var(--mk-text)         — основной текст
var(--mk-text-2)       — вторичный текст
var(--mk-text-3)       — третичный / disabled текст
var(--mk-border)       — граница
var(--font-geist-sans) — основной шрифт
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать NavBar Client Component</name>
  <files>src/components/ui/nav-bar.tsx</files>
  <action>
    Создать файл src/components/ui/nav-bar.tsx — 'use client' компонент.

    Импорты:
    - Link из 'next/link'
    - usePathname из 'next/navigation'
    - logout из '@/app/actions/auth'

    Логика активного состояния:
    - const pathname = usePathname()
    - isPricesActive = pathname === '/prices'
    - isOrdersActive = pathname.startsWith('/orders')

    JSX структура: nav с display flex, alignItems center, justifyContent space-between, paddingTop 20px, paddingBottom 16px.

    Левая часть (навигация): div с display flex, gap 6px. Два Link: href='/prices' label='Прайс', href='/orders' label='Заказы'.
    Стиль активной ссылки: padding 7px 14px, borderRadius 8px, fontSize 13px, fontWeight 700, background rgba(200,67,26,0.12), color var(--mk-accent), border 1px solid rgba(200,67,26,0.2), textDecoration none.
    Стиль неактивной ссылки: те же размеры, fontWeight 500, background transparent, color var(--mk-text-2), border 1px solid transparent, textDecoration none.
    Добавить fontFamily var(--font-geist-sans) на Link элементы.

    Правая часть (выход): form с action={logout}. Кнопка type="submit" с текстом «Выйти».
    Стиль кнопки: padding 7px 14px, borderRadius 8px, fontSize 13px, fontWeight 500, background transparent, border 1px solid var(--mk-border), color var(--mk-text-3), cursor pointer, fontFamily var(--font-geist-sans).

    Никаких useRouter, никаких Tailwind-классов — только inline styles (паттерн проекта).
  </action>
  <verify>
    npx tsc --noEmit 2>&1 | head -30
  </verify>
  <done>Файл создан, TypeScript-ошибок нет, компонент экспортирован как default.</done>
</task>

<task type="auto">
  <name>Task 2: Подключить NavBar на 4 защищённые страницы</name>
  <files>
    src/app/prices/page.tsx,
    src/app/orders/page.tsx,
    src/app/orders/new/page.tsx,
    src/app/orders/[id]/page.tsx
  </files>
  <action>
    Добавить `import NavBar from '@/components/ui/nav-bar'` в каждый файл.
    Разместить `&lt;NavBar /&gt;` первым элементом внутри внутреннего div (maxWidth 480px...) — перед всем остальным содержимым.

    src/app/prices/page.tsx:
    - Добавить &lt;NavBar /&gt; перед &lt;header&gt;
    - В &lt;header&gt;: убрать paddingTop: 48px (NavBar занял верхнее пространство), оставить paddingBottom: 24px

    src/app/orders/page.tsx:
    - Добавить &lt;NavBar /&gt; перед &lt;header&gt;
    - В &lt;header&gt;: убрать paddingTop: 48px, оставить paddingBottom: 24px

    src/app/orders/new/page.tsx:
    - Удалить текущий div-header (flex, alignItems center, justifyContent space-between, padding 24px 0 20px) с h1 «Новый заказ» и Link «← Заказы»
    - Добавить &lt;NavBar /&gt; на его место
    - После NavBar — h1 «Новый заказ» с marginTop 4px, fontSize 22px, fontWeight 700, color var(--mk-text), letterSpacing -0.02em

    src/app/orders/[id]/page.tsx:
    - Добавить &lt;NavBar /&gt; перед &lt;header&gt;
    - В &lt;header&gt;: убрать paddingTop: 48px; убрать блок Link «← Назад» с marginBottom 18px (навигация теперь в NavBar)

    Ни в одном файле не трогать бизнес-логику (verifySession, data fetching, render-логику заказов).
  </action>
  <verify>
    npx tsc --noEmit 2>&1 | head -30
  </verify>
  <done>
    TypeScript-ошибок нет.
    NavBar присутствует в каждом из 4 файлов.
    Страницы prices/orders не имеют paddingTop 48px на header.
    Страница orders/new не имеет ручной «← Заказы» ссылки.
    Страница orders/[id] не имеет «← Назад» ссылки.
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit
npm run build 2>&1 | tail -20
</verification>

<success_criteria>
- src/components/ui/nav-bar.tsx существует с 'use client', usePathname, form action={logout}
- Все 4 страницы импортируют и рендерят NavBar
- npx tsc --noEmit без ошибок
- Кнопка «Выйти» работает через form action={logout} без JS
- Активное состояние: isPricesActive = pathname === '/prices', isOrdersActive = pathname.startsWith('/orders')
</success_criteria>

<output>
После выполнения создать `.planning/phases/03.1-navigation-auth-ux/03.1-01-SUMMARY.md` с описанием реализованного NavBar, изменений в каждом файле и любых отклонений от плана.
</output>
