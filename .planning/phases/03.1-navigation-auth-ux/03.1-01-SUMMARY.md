---
phase: 03.1-navigation-auth-ux
plan: 01
subsystem: ui
tags: [next.js, react, navigation, auth, logout, usePathname]

# Dependency graph
requires:
  - phase: 03-orders
    provides: Protected pages (prices, orders, orders/new, orders/[id]) that need navigation
  - phase: 01-foundation
    provides: logout() Server Action in src/app/actions/auth.ts
provides:
  - NavBar Client Component with active-link highlighting and form-based logout
  - Navigation between /prices and /orders on all 4 protected pages
  - AUTH-04 closure — logout button accessible from every protected page
affects: [03.1-02-prices-form, any future protected pages]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "NavBar as 'use client' component using usePathname for active-link detection"
    - "form action={logout} for progressive-enhancement logout (works without JS)"
    - "Inline styles only — no Tailwind (project convention)"

key-files:
  created:
    - src/components/ui/nav-bar.tsx
  modified:
    - src/app/prices/page.tsx
    - src/app/orders/page.tsx
    - src/app/orders/new/page.tsx
    - src/app/orders/[id]/page.tsx

key-decisions:
  - "NavBar replaces paddingTop: 48px on header — NavBar itself provides top spacing"
  - "orders/new: manual flex-header with h1 and back link replaced by NavBar + standalone h1"
  - "orders/[id]: back link removed — NavBar provides access to /orders"

patterns-established:
  - "Active nav detection: isPricesActive = pathname === '/prices', isOrdersActive = pathname.startsWith('/orders')"
  - "Logout via <form action={logout}> — Server Action, no client-side JS required"

requirements-completed: [AUTH-04]

# Metrics
duration: ~15min
completed: 2026-05-04
---

# Phase 03.1 Plan 01: NavBar + Logout Summary

**'use client' NavBar с usePathname active-link highlighting и form action={logout}, подключённый на все 4 защищённые страницы (/prices, /orders, /orders/new, /orders/[id])**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-04
- **Completed:** 2026-05-04
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Создан NavBar Client Component с навигацией [Прайс] [Заказы] и подсветкой активного раздела
- Кнопка «Выйти» через `<form action={logout}>` — работает без JS (progressive enhancement)
- NavBar добавлен на все 4 защищённые страницы, закрыт AUTH-04

## Task Commits

Каждая задача зафиксирована атомарно:

1. **Task 1: Создать NavBar Client Component** — `3ff5b78` (feat)
2. **Task 2: Подключить NavBar на 4 защищённые страницы** — `297c295` (feat)

## Files Created/Modified

- `src/components/ui/nav-bar.tsx` — 'use client' компонент: usePathname, два Link, form action={logout}, inline styles
- `src/app/prices/page.tsx` — добавлен `<NavBar />`, убран paddingTop: 48px с header
- `src/app/orders/page.tsx` — добавлен `<NavBar />`, убран paddingTop: 48px с header
- `src/app/orders/new/page.tsx` — удалён ручной flex-header с ссылкой «← Заказы», добавлен NavBar + отдельный h1
- `src/app/orders/[id]/page.tsx` — добавлен `<NavBar />`, убраны paddingTop: 48px и ссылка «← Назад»

## Decisions Made

- NavBar занимает верхнее пространство вместо `paddingTop: 48px` на header — убрали отступ из header
- На странице orders/new полностью заменили старый div-header (с h1 + back link) на NavBar + отдельный h1 после него
- На странице orders/[id] убрали ссылку «← Назад» — NavBar с разделом «Заказы» обеспечивает навигацию обратно

## Deviations from Plan

None — план выполнен точно как написан.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- NavBar готов, все защищённые страницы имеют навигацию и кнопку выхода
- Готово к 03.1-02: PricesForm Client Component (замена form на странице /prices на 'use client' с оптимистичными обновлениями)

---
*Phase: 03.1-navigation-auth-ux*
*Completed: 2026-05-04*
