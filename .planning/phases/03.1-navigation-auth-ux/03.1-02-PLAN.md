---
phase: 03.1-navigation-auth-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 03.1-01
files_modified:
  - src/components/ui/prices-form.tsx
autonomous: false
requirements:
  - AUTH-04

must_haves:
  truths:
    - "После успешного сохранения цен появляется кнопка «Создать заказ →» и не исчезает через 2.6 сек"
    - "Кнопка «Создать заказ →» ведёт на /orders/new"
    - "Success-баннер «Цены сохранены» остаётся виден вместе с кнопкой до следующего изменения цен"
  artifacts:
    - path: "src/components/ui/prices-form.tsx"
      provides: "PricesForm с постоянным success state и кнопкой навигации"
      contains: "href=\"/orders/new\""
  key_links:
    - from: "src/components/ui/prices-form.tsx"
      to: "/orders/new"
      via: "Link href=/orders/new в блоке showSuccess"
      pattern: "href.*orders/new"
---

<objective>
Модифицировать PricesForm: убрать автоскрытие success state через setTimeout, добавить кнопку «Создать заказ →» в success-блок. Верификация в браузере.

Purpose: После сохранения цен пользователь должен иметь возможность нажать «Создать заказ» — кнопка не должна исчезать через 2.6 сек.

Output: Обновлённый prices-form.tsx + прохождение browser verify checkpoint
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-navigation-auth-ux/03.1-01-SUMMARY.md
@.planning/phases/03.1-navigation-auth-ux/03.1-RESEARCH.md

<interfaces>
<!-- Текущая логика showSuccess в prices-form.tsx -->
<!-- ПРОБЛЕМА: setTimeout(2600ms) скрывает success-баннер до того, как пользователь успеет нажать кнопку -->

Текущий useEffect (строки 53-59):
```typescript
useEffect(() => {
  if (saveState?.success) {
    setShowSuccess(true)
    const t = setTimeout(() => setShowSuccess(false), 2600)  // УБРАТЬ этот таймаут
    return () => clearTimeout(t)
  }
}, [saveState])
```

Текущий showSuccess JSX (строки 247-267):
```typescript
{showSuccess && (
  <div style={{ /* green success banner */ }}>
    <CheckIcon />
    Цены сохранены
  </div>
)}
```

После изменения:
- Убрать setTimeout, showSuccess сбрасывается только при следующем saveState изменении
- Добавить Link «Создать заказ →» после success-баннера (внутри того же showSuccess блока)
- Link стиль: полная ширина 100%, height 50px, borderRadius 14px, gradient акцент, белый текст, fontWeight 600, textDecoration none, fontFamily var(--font-geist-sans), boxShadow var(--mk-accent-glow)
- animationName mkUp, animationDuration 0.3s, animationFillMode both (паттерн анимации из проекта)

Import: добавить Link из 'next/link' в начало файла (уже есть React-импорты).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Убрать auto-hide и добавить кнопку «Создать заказ» в PricesForm</name>
  <files>src/components/ui/prices-form.tsx</files>
  <action>
    1. Добавить `import Link from 'next/link'` в начало файла (после существующих импортов).

    2. Изменить useEffect для showSuccess (строки 53-59):
       - Убрать строки с setTimeout и clearTimeout
       - Оставить только: `if (saveState?.success) { setShowSuccess(true) }`
       - Новый useEffect:
         ```typescript
         useEffect(() => {
           if (saveState?.success) {
             setShowSuccess(true)
           }
         }, [saveState])
         ```

    3. В JSX блок showSuccess (после «Цены сохранены» div) — обернуть в Fragment и добавить Link:
       ```typescript
       {showSuccess && (
         <>
           <div style={{ /* существующий success banner без изменений */ }}>
             <CheckIcon />
             Цены сохранены
           </div>
           <Link
             href="/orders/new"
             style={{
               display: 'flex',
               alignItems: 'center',
               justifyContent: 'center',
               width: '100%',
               height: '50px',
               borderRadius: '14px',
               background: 'linear-gradient(135deg, var(--mk-accent) 0%, var(--mk-accent-hi) 100%)',
               color: 'white',
               fontSize: '15px',
               fontWeight: 600,
               textDecoration: 'none',
               letterSpacing: '-0.01em',
               fontFamily: 'var(--font-geist-sans)',
               boxShadow: '0 6px 24px var(--mk-accent-glow)',
               animationName: 'mkUp',
               animationDuration: '0.3s',
               animationFillMode: 'both',
               marginBottom: '10px',
             }}
           >
             Создать заказ →
           </Link>
         </>
       )}
       ```

    4. Не трогать остальную логику компонента — только useEffect и showSuccess JSX блок.
  </action>
  <verify>
    npx tsc --noEmit 2>&1 | head -20
  </verify>
  <done>
    TypeScript-ошибок нет.
    В файле нет setTimeout связанного с setShowSuccess.
    В файле есть import Link from 'next/link'.
    В блоке showSuccess есть Link с href="/orders/new".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verify — NavBar и logout на всех страницах</name>
  <action>Запустить dev-сервер: npm run dev. Проверить NavBar и logout по шагам ниже.</action>
  <verify>Пользователь подтверждает все 5 шагов проверки в браузере.</verify>
  <done>Пользователь написал "approved".</done>
  <what-built>
    1. NavBar компонент на всех 4 защищённых страницах (/prices, /orders, /orders/new, /orders/[id])
    2. Кнопка «Выйти» в NavBar вызывает Server Action logout
    3. Кнопка «Создать заказ →» появляется после сохранения цен и не исчезает автоматически
  </what-built>
  <how-to-verify>
    Запустить dev-сервер: npm run dev

    1. Открыть http://localhost:3000/prices
       - Видна навигация вверху: [Прайс] [Заказы] и кнопка [Выйти]
       - Раздел «Прайс» подсвечен (акцентный цвет)
       - Раздел «Заказы» не подсвечен (серый)

    2. Нажать [Заказы] в NavBar
       - Переход на /orders
       - Раздел «Заказы» подсвечен, «Прайс» нет

    3. Нажать [+ Новый заказ] → /orders/new
       - NavBar присутствует, «Заказы» подсвечен (startsWith '/orders')
       - Нет ручной «← Заказы» ссылки

    4. Вернуться на /prices, сохранить любые цены
       - Появляется «Цены сохранены» и «Создать заказ →»
       - Подождать 5 секунд — кнопка НЕ исчезает
       - Нажать «Создать заказ →» — переход на /orders/new

    5. На любой странице нажать [Выйти]
       - Перенаправление на /login
       - Повторное открытие /prices → снова /login (сессия удалена)
  </how-to-verify>
  <resume-signal>Написать "approved" или описать проблемы</resume-signal>
</task>

</tasks>

<verification>
npx tsc --noEmit
grep -n "setTimeout" src/components/ui/prices-form.tsx
grep -n "orders/new" src/components/ui/prices-form.tsx
</verification>

<success_criteria>
- setTimeout для showSuccess удалён из prices-form.tsx
- Кнопка «Создать заказ →» появляется в showSuccess блоке и не исчезает автоматически
- Browser verify пройдена: NavBar работает на всех страницах, logout работает
- npx tsc --noEmit — 0 ошибок
</success_criteria>

<output>
После выполнения создать `.planning/phases/03.1-navigation-auth-ux/03.1-02-SUMMARY.md` с подтверждением browser verify, описанием изменений в prices-form.tsx и финальным статусом фазы 3.1.
</output>
