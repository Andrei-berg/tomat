---
phase: 04.1-partial-payments-at-order-creation
plan: "02"
subsystem: ui
tags: [react, next.js, form, debt, prepayment]

# Dependency graph
requires:
  - phase: 04.1-partial-payments-at-order-creation
    provides: "04.1-01 — createOrder Server Action extended with prepayment_amount + prepayment_type handling, debt_payments insert, and orders.status update"
provides:
  - "OrderForm with optional prepayment section visible only for debt orders"
  - "prepaymentAmount / prepaymentType state wired into buildFormData() and resetForm()"
affects: [reports, debt-management]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Conditional JSX section pattern — {paymentType === 'debt' && <div>...} used for prepayment UI"
    - "max={effective} on numeric input to enforce UI-level cap (server also enforces via Math.min)"

key-files:
  created: []
  modified:
    - src/components/ui/order-form.tsx

key-decisions:
  - "Prepayment amount input max is set to effective total — matches server-side Math.min cap (defence in depth)"
  - "prepaymentType defaults reset to 'cash' on form reset — consistent with server default guard"
  - "Prepayment section not shown on success screen — RESEARCH explicitly marked this non-critical"

patterns-established:
  - "Section 4.5 Prepayment inserted between Client section and Total+Save section"

requirements-completed:
  - PREPAY-01

# Metrics
duration: 2min
completed: 2026-05-04
---

# Phase 4.1 Plan 02: OrderForm Prepayment UI Summary

**Optional prepayment section added to OrderForm — amount input capped by effective total + Нал/Карта selector, visible only for debt orders, wired into buildFormData() and cleared on reset**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-04T18:19:08Z
- **Completed:** 2026-05-04T18:20:22Z
- **Tasks:** 1 of 1 auto tasks (checkpoint:human-verify pending)
- **Files modified:** 1

## Accomplishments
- Added `prepaymentAmount` and `prepaymentType` state variables to `OrderForm`
- Extended `buildFormData()` to conditionally include prepayment fields when `paymentType === 'debt'` and amount > 0
- Extended `resetForm()` to clear both prepayment state vars on "Новый заказ"
- Added Section 4.5 Prepayment JSX between Client and Total+Save sections, hidden for cash/card orders
- Amount input has `max={effective}` and placeholder showing the cap

## Task Commits

Each task was committed atomically:

1. **Task 1: Add prepayment state, buildFormData extension, and resetForm update** - `c8a5b84` (feat)

**Plan metadata:** pending final doc commit

## Files Created/Modified
- `src/components/ui/order-form.tsx` - OrderForm extended with prepayment section, state, buildFormData and resetForm updates

## Decisions Made
- Prepayment amount input max attribute set to `effective` (the computed effective total) — defence-in-depth alongside server-side `Math.min` cap established in 04.1-01
- Prepayment type selector uses same button pattern as the main payment type selector for visual consistency
- Post-success screen does NOT show prepayment details — plan research explicitly marked this non-critical

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Full prepayment-at-order-creation feature is code-complete (04.1-01 backend + 04.1-02 UI)
- Browser verification (checkpoint:human-verify) is the remaining gate before phase 04.1 can be marked done
- After browser approval, phase 05 (Reports) is next

---
*Phase: 04.1-partial-payments-at-order-creation*
*Completed: 2026-05-04*
