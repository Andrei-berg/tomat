---
phase: 05-reports-export
plan: "04"
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified: []
autonomous: false
requirements:
  - REPORT-01
  - REPORT-02
  - REPORT-03
  - REPORT-04

must_haves:
  truths:
    - "User can open /report and see revenue breakdown by payment type for a selected period"
    - "User can see product breakdown table with boxes, kg, amount per product"
    - "User can download a .xlsx file that opens in Excel with numeric cells (not text)"
    - "User can download a .pdf file that renders payment summary and product table"
    - "NavBar shows Отчёты link that is highlighted when on /report"
  artifacts:
    - path: "src/app/report/page.tsx"
      provides: "/report page"
      contains: "getReportData"
    - path: "src/app/api/report/excel/route.ts"
      provides: "Excel export"
      exports: ["GET"]
    - path: "src/app/api/report/pdf/route.ts"
      provides: "PDF export"
      exports: ["GET"]
  key_links:
    - from: "browser /report"
      to: "/api/report/excel"
      via: "Excel (.xlsx) anchor link with from/to params"
      pattern: "download"
    - from: "browser /report"
      to: "/api/report/pdf"
      via: "PDF anchor link with from/to params"
      pattern: "download"
---

<objective>
Browser verification checkpoint — confirm all four report requirements work end-to-end in the running application.

Purpose: Plans 01-03 built the data layer, UI, and export handlers. This checkpoint confirms they work together correctly with real data in the browser.
Output: User approval that reports display correctly and both file exports download successfully.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and print verification URL</name>
  <files></files>
  <action>
    Ensure the development server is running:
    ```
    npm run dev
    ```
    If already running, skip. Print the URL to visit:
    - http://localhost:3000/report

    Confirm the build compiled without errors (npm run build was run in Plan 03).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/report 2>/dev/null || echo "server not running — start with: cd /home/user/Projects/tomat && npm run dev"</automated>
  </verify>
  <done>Dev server responds to http://localhost:3000/report (may redirect to /login if not authenticated — that is expected).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 5 Reports feature:
    - /report page with date range picker, revenue summary, product breakdown table
    - NavBar updated with "Отчёты" link (active state on /report)
    - GET /api/report/excel — downloads .xlsx with two sheets
    - GET /api/report/pdf — downloads .pdf with payment summary and product table
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000 in browser — log in if needed
    2. Verify NavBar shows "Отчёты" link on /prices, /orders, and /debts pages
    3. Click "Отчёты" — confirm navigation to /report; "Отчёты" appears highlighted in nav
    4. Confirm the page shows date range (default: first day of current month → today)
    5. REPORT-01: Verify revenue table shows Наличные / Карта / Долги (не получено) / Долги (получено) / Итого
    6. REPORT-02: Verify product breakdown table shows product rows with Ящики, кг, Сумма columns
    7. If no orders in current month: change "С" date to an earlier month with known orders — confirm data updates
    8. REPORT-03: Click "Excel (.xlsx)" button — file downloads; open in Excel/LibreOffice Calc — confirm:
       - Two sheets: "Выручка" and "Товары"
       - Numbers are numeric (can be summed in Excel, not stored as text)
    9. REPORT-04: Click "PDF" button — file downloads; open in PDF viewer — confirm:
       - Shows report title with date range
       - Payment summary section visible
       - Product table visible (if there are orders in range)
    10. If either export fails: note the error message and describe it in your response
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All four REPORT requirements verified by human:
- REPORT-01: Revenue breakdown visible on /report
- REPORT-02: Product breakdown table visible on /report
- REPORT-03: Excel file downloads with numeric cells
- REPORT-04: PDF file downloads with readable layout
</verification>

<success_criteria>
User types "approved" — all four report requirements verified in browser.
Phase 05 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reports-export/05-04-SUMMARY.md`
</output>
